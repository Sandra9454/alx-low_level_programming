/**
 * _strcmp - Compare two strings
 * @s1: The first string
 * @s2: The second string
 *
 * Return: Less than 0 if s1 is less than s2, 0 if they're equal
 * or more than 0 if s1 is greater than s2
 */
int _strcmp(char *s1, char *s2)
{
	(void)s1;
	(void)s2;
	return (0);
}
